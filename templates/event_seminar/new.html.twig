{% extends 'base.html.twig' %}

{% block title %}Nuevo Evento · {{ event_seminar.seminar.name }}{% endblock %}

{% block body %}
<div class="page-wrap">

    <div class="page-header">
        <a href="{{ path('app_seminario_index') }}" class="breadcrumb-link">← Seminarios</a>
        <h1 class="page-title">Nuevo evento</h1>
        <p class="page-subtitle">{{ event_seminar.seminar.name }}</p>
    </div>

    {{ include('event_seminar/_form.html.twig') }}

</div>
{% endblock %}
