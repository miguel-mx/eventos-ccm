{% extends 'base.html.twig' %}

{% block title %}Edit EventSeminar{% endblock %}

{% block body %}
    <div class="container my-5">

        <div class="row justify-content-center">
            <div class="col-12 col-md-8 col-lg-6 mb-5">
                <h2>{{ event_seminar.title }}</h2>
            </div>
        </div>

        {{ include('event_seminar/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_event_seminar_index') }}">back to list</a>

        {{ include('event_seminar/_delete_form.html.twig') }}
    </div>
{% endblock %}
