{% extends 'base.html.twig' %}

{% block title %}Editar Seminario · CCM{% endblock %}

{% block body %}
<div class="page-wrap">

    <div class="page-header">
        <a href="{{ path('app_seminario_index') }}" class="breadcrumb-link">← Seminarios</a>
        <h1 class="page-title">Editar seminario</h1>
    </div>

    <div class="form-wrap">
        {{ include('seminario/_form.html.twig', {'button_label': 'Guardar cambios'}) }}
    </div>

    <div class="page-actions">
        <a href="{{ path('app_seminario_index') }}" class="action-link">← Cancelar</a>
        {{ include('seminario/_delete_form.html.twig') }}
    </div>

</div>
{% endblock %}
